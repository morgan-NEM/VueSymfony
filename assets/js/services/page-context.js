/**
 * Retourne l'id de la catégorie courante donnée par le server
 * @returns {string|null|*}
 */
export function getCurrentCategoryId() {
    console.log(window.currentCategoryId);
    return window.currentCategoryId;
}
